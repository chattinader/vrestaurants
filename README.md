# vrestaurants
Application VueJS en mode CLI
Ce site contient une liste de restaurants présentés sous forme d'un tableau sur plusieurs pages.
Vous pouvez visualiser les informations de chaque restaurant (nom, type de cuisine, classe et adresse), ainsi que
la position GPS sur une carte GoogleMap.
Vouv avez la possibilité de soit modifier les informationsd'un restaurant existant,de supprimer
ou d'ajouter un restaurant.
Pour rechercher un restaurant dans le tableau il suffit de rentrer le nom de ce dernier dans la barre de recherhce.
Ce Site est une application VueJS développée en Mode CLI.

-------------Lancement de l'application----------------------------------------------------------------------------
Pour lancer le projet il faut:
dans un premier terminal
-exécuter la commande "npm install" (qui installe les paquets requis)
-accéder au dossier serveur
-exécuter la commande "node serverCrudWithMongo.js" (qui lance le serveur)

dans un deuxième terminal
-exécuter la commande "npm run dev" (qui lance l'application)
-aller dans un navigateur à l'adresse indiquée sur le deuxième terminal après le lancement de
l'application (localhost:8081)

!!Important!!
lancer le serveur en premier puis l'application pour ne pas créer un confusion sur les ports

-------------A propos----------------------------------------------------------------------------------------------
Développeur			Université					Promo
CHATTI Nader			Université de Nice Sophia Antipolis		M1 MIAGE 2018/2019